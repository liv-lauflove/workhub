# Graphify Knowledge Graph Rules

- After modifying code files, you MUST run `graphify update .` to keep the Knowledge Graph in sync.
- Use the knowledge graph for context to prevent hallucinations and understand the project architecture.
